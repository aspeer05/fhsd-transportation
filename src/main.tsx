import React from 'react'
import ReactDOM from 'react-dom/client'
import FHSDTransportation from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FHSDTransportation />
  </React.StrictMode>
)
